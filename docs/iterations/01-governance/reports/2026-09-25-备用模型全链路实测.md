# 2026-09-25 备用模型全链路实测

**仅用备用服务的新全量任务已跑通；回答质量检查仍有两项待改进。** 浏览器发起请求、七个真实 Hadoop 作业、结果发布、自动解释、产物下载、分区读取及 API 重启验证均通过。五项补充追问首测 4/5 通过，异常样例题同题重试后通过；另一次要求简短的追问输出了整份报告。首次检查并非全部通过。

机器摘要见[同名 JSON](2026-09-25-备用模型全链路实测.json)，包含每次模型尝试、检查结果、文件校验值及原始证据摘要。本次没有调整清洗规则、评分方法或应用 Python 代码；加强了全链路验收脚本的服务路由与证据检查。

## 1. 环境与范围

| 项目 | 实际配置或记录 |
| --- | --- |
| 系统用户 / Python | `duscwalk` / AgentDev，Python 3.11.16 |
| 验收代码 commit | `20b55cc5d55b763abfd5ac8b8c094121069bde16` |
| Python 实现摘要 | `sha256-fb44a805e7dbd945fe55fce3c554a5d57e7d178adf89ce742513f3e6c172bf39` |
| 模型路由 | 本地 `.env` 设为 `MODEL_PROVIDER=backup`，重启 API 后核对 |
| 实际备用模型 | `deepseek-v4-flash`；90 秒网络超时、4096 token 输出上限 |
| Hadoop | 3.5.0 / JDK 17，1 个存活 DataNode、1 个 RUNNING NodeManager |
| 会话 | `94c8e0627f004038b75fd28c94af2e37` |
| 新任务 | `064523f8a8bf41a38f7c2193caf59930` |
| 普通测试 | 81 项发现，79 通过、2 个需显式启用的 Hadoop 测试跳过，4.489 秒 |

准备时发现 NodeManager 已停止，日志包含 SIGTERM；恢复该进程后确认节点为 RUNNING。NameNode、DataNode、ResourceManager 和既有存储保留，未格式化或清空。普通测试与下面真实全量 Hadoop 运行分别计入证据。

本轮 9 次 Agent 请求共 **28 次模型尝试，全部由备用服务完成，主服务尝试为 0**，未出现模型网络错误。这只证明本次运行期间可用；没有复测主服务，也没有验证自动故障切换或长期可靠性。`.env` 继续忽略并保持私有权限，报告不记录服务密钥或地址。

## 2. 新的浏览器 → Hadoop → 解释链路

浏览器验收从北京时间 **10:56:32** 开始，到首次追问完成为 **11:09:31**，共 **778.952 秒**。任务从受理到正式发布为 **685.613409 秒**，约 11.4 分钟。以下耗时仅是这一次的观测。

| 环节 | 实际结果 |
| --- | --- |
| 自然语言提交 | HTTP 200，真实工具受理回执；1 轮模型调用，模型耗时 30.264 秒 |
| 任务执行 | `succeeded`；准备、七作业、导出共九阶段成功 |
| 发布前隔离 | 在 `after-groups` 阶段检查：可见产物 0，质量接口 HTTP 409 |
| 自动完整解释 | HTTP 200，`evidence_rendered`；3 轮模型调用，模型耗时 17.414 秒；包含评分、处置、评分损失、时效、时间边界和局限 |
| 浏览器首次追问 | HTTP 200，使用本轮精确 summary；4 轮模型调用，模型耗时 32.619 秒；未重复提交治理任务 |
| 浏览器错误 | 无页面脚本错误；该会话仅创建 1 个治理任务 |

七作业的外部状态均向 ResourceManager 实际核对：

| 阶段 | YARN application ID | 最终状态 |
| --- | --- | --- |
| before-parents | `application_1790268267118_0008` | FINISHED / SUCCEEDED |
| before-ratings | `application_1790268267118_0009` | FINISHED / SUCCEEDED |
| before-metrics | `application_1790268267118_0010` | FINISHED / SUCCEEDED |
| clean-parents | `application_1790268267118_0011` | FINISHED / SUCCEEDED |
| clean-ratings | `application_1790268267118_0012` | FINISHED / SUCCEEDED |
| after-groups | `application_1790268267118_0013` | FINISHED / SUCCEEDED |
| after-metrics | `application_1790268267118_0014` | FINISHED / SUCCEEDED |

## 3. 数据、文件与后续读取

| 表 | 输入 | 输出 | 保留 | 修复后输出 | 去重 | 隔离 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| users | 6,946 | 5,895 | 5,895 | 0 | 118 | 933 |
| movies | 4,465 | 3,836 | 3,818 | 18 | 117 | 512 |
| ratings | 1,150,241 | 935,354 | 935,354 | 0 | 0 | 214,887 |

| 维度 | 清洗前 | 清洗后 |
| --- | ---: | ---: |
| Accurate | 95.406801 | 100.000000 |
| Complete | 98.238324 | 100.000000 |
| Unique | 88.405981 | 100.000000 |
| Consistent | 78.308223 | 100.000000 |
| Up-to-date | 1.884996 | 2.160465 |

四项满分仅表示保留数据通过已实现约束，不能证明真实属性或下一轮模型效果。R12 父表引用问题共 **89,939 次可重叠原因命中**，不是互斥行数；评分减少来自隔离，评分去重为 0。时效性为 `20,208 / 935,354 × 100 = 2.160465 分`，使用原报告固定历史窗口。

全量文件检查耗时 **20.018 秒**。三张清洗表、处置明细、`quality.json`、`report.md` 和 `dataset-manifest.json` 共 **7 个文件**均计算本地 SHA-256，并通过 HTTP 完整流式下载核对字节数和哈希。质量文件 SHA-256 为 `e16f6a7bc29ce674aa36c2b3b0af8af1f3015d7b436fe0703410dd3a67aa30c5`，其余摘要见机器记录。

`CleanedDataset` 校验固定产物、清单、配置和三文件后，逐行读取全部评分分区，核对行数、合法评分与用户/电影外键；处置明细守恒及实体数也通过。实际 train / validation / test 行数分别为 **746,139 / 100,814 / 88,401**。T1 为 `975628799`（训练包含），T2 为 `978307199`（验证包含）；分区仍是过滤条件，**未生成三个独立分区文件**。

| 引用 | 固定版本 |
| --- | --- |
| 原始数据 | `ml-1m.raw@sha256-46bfa0020d750da32d409f93c6cb305a1347019f8a703f7f6b943458ee0fa578` |
| 本轮清洗数据 | `064523f8a8bf41a38f7c2193caf59930.cleaned@sha256-a21c9360829ab79d631da78c23b3746dbb579cb8cef317826ab085daf47a11ac` |
| 本轮质量报告 | `064523f8a8bf41a38f7c2193caf59930.quality@sha256-99270913bd23aa3024e2f1ad5044748379d72c7b4d0a6d6b24692511653d7038` |

本轮三张清洗文件与历史任务 `33ce0c793b6e4d92acb6400cd96b3faa` 的对应哈希完全一致；任务与清单引用不同。已有[固定版本交接指南](../handoff/清洗数据读取.md)保持其原先的引用，不混用两次任务。

## 4. 补充追问与未通过项

在同一个真实会话中，经 HTTP API 顺序发出五题，均使用新请求 ID，不重跑 Hadoop。每题检查本任务的精确报告、本轮 summary 工具调用、所需解释要点、备用服务路由及任务数未增加。

| 题目 | 首测请求耗时 | 首测结果 |
| --- | ---: | --- |
| 父表原因重叠与评分损失 | 18.656 秒 | 通过 |
| 时效性公式与单位 | 50.140 秒 | 通过 |
| 时间边界与分区文件 | 36.797 秒 | 通过 |
| 事实真实性与下游效果承诺 | 20.002 秒 | 通过 |
| 异常样例、处置及来源 | 53.351 秒 | **未通过**；已取到样例，但回答未展示 |

样例首测先成功读取 `ratings/R14_VALID_KEY_CONFLICT` 的三条真实记录，并一度选中样例要点；补读强制要求的质量 summary 后，最终选了 12 个通用报告要点，遗漏用户所需样例。工具返回数据有效、协议校验通过且 HTTP 200，仍不代表回答满足问题。验收脚本正确保留 `current_examples=false` 并以失败退出。

保持代码、模型配置、数据、会话和题目相同，仅换新请求 ID 重试这一题，**50.075 秒后通过**：选择本轮样例要点，展示原始行、`quarantined` 处置、原始数据版本、文件名、行号和字节偏移。初次三个样例与重试样例都按来源偏移及行号回读原始文件，内容一致。首测失败记录保留；重试成功不证明问题已修复。

另一个问题来自浏览器首次追问：用户要求简短回答，模型仍选了 **12 个要点，输出 3,623 字符**；自动完整解释为 1,486 字符。事实约束可以限制内容来自报告，却没有保证答复的相关性、覆盖范围和简洁程度。这些问题已列入后续开发待办，当前应用逻辑未变。

## 5. 页面与重启持久性

- 桌面 1440×1000、手机 390×844 检查通过：五维指标、行数、清洗样例、异常来源样例、质量下载哈希及新会话清空均符合实际 API 结果，页面无脚本错误。
- 对本轮自动解释检查“报告事实 · 要点选择模型”标签、精确 validation、摘要调用 ID 与刷新后显示；手机页面无整体横向溢出。
- 确认全局无处理中模型请求后，优雅停止并重启 API。**18 条消息、9 条 Agent 回复、任务、质量摘要和全部调用记录的规范化 JSON 哈希完全一致**；catalog 总任务数仍为 3，本会话仍为 1，配置仍为备用服务。
- 重启后对真实样例重试回复再次运行页面证据检查，通过。两次证据页面检查为 `persisted-reply`，只验证已有真实回复的呈现，不额外计算为新模型请求。

## 6. 复现与证据位置

按[应用指南](../../../development/app-and-model.md)准备 AgentDev、模型配置、Hadoop、worker 和 API。仅使用备用模型时在私有 `.env` 中设置 `MODEL_PROVIDER=backup` 并重启 API。随后运行：

```bash
python -m unittest discover -s tests -q
NODE_PATH="$PWD/var/browser-check/node_modules" \
  MOVIELENS_EXPECT_PROVIDER=backup \
  MOVIELENS_E2E_OUTPUT=var/verification/backup-full-e2e \
  node scripts/checks/natural_language_e2e.cjs
```

这会创建一个新的全量任务，运行时间约十余分钟；使用自己的新会话与任务引用。Playwright 安装步骤见应用指南。本机实际使用已有缓存中的 Playwright，模型与 Hadoop 均为真实服务。

只复核已有任务的追问时，停止 API 和其他独立 Agent 脚本，使用仓库中的 [explanation_regression.py](../../../../scripts/checks/explanation_regression.py)；`--case example --output 新目录` 可单独重试样例并保留失败证据。该入口持有 API 同一把锁，不能与 API 同时运行。本轮补充题通过 API 调用，所用本地校验脚本也登记了摘要。

本地原始证据位于 `var/verification/2026-09-25/`，大型记录、运行数据和截图不进入 Git：

| 目录 | 内容 |
| --- | --- |
| `backup-full-e2e/` | 浏览器提交、任务九阶段、28 次模型调用、补充题首测及重试、文件/分区/来源/重启校验 |
| `backup-full-browser/` | 桌面与手机页面、真实样例、下载和会话切换 |
| `backup-evidence-browser/` | 自动解释的来源标签、调用依据与刷新 |
| `backup-evidence-after-restart/` | API 重启后样例回复的显示、证据与刷新 |

浏览器复查入口：<http://127.0.0.1:8765/?session=94c8e0627f004038b75fd28c94af2e37>。该入口依赖本机 catalog 和服务。

后续优先改进追问切题与简洁性，并在相同问题上重复验证；另一位成员独立复现、演示内容审阅及约 10 分钟演练仍待完成。功能分支继续保留，不因本机主流程通过就合并或创建正式交付标签。

返回[迭代一导航](../README.md)。
